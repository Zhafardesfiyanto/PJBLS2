// Basic smoke test — verifies the app entry point compiles correctly.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder smoke test', () {
    expect(1 + 1, equals(2));
  });
}
