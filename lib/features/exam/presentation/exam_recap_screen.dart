import 'package:flutter/material.dart';

/// Screen showing exam results and gesture recap
class ExamRecapScreen extends StatelessWidget {
  const ExamRecapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Exam Recap - To be implemented'),
    );
  }
}